# Frontend fix v8 — visual QA corrections

## Исправлено

1. Убрано дублирование `BR` в левом верхнем углу:
   - в боковом rail оставлен один компактный бренд-маркер `B`;
   - из верхней панели удалён второй визуальный `BR`.

2. Переразложен левый столбец основного контента:
   - очередь 15m-рекомендаций поднята выше операционной формы;
   - панель параметров рынка и операций по умолчанию свернута;
   - форма операций больше не съедает первый экран;
   - для sticky-left/right колонок добавлен собственный вертикальный скролл.

3. Исправлены проблемы плотности и переполнения:
   - candidate row больше не ломается на узком левом столбце;
   - скрыт декоративный `star`, который расходовал полезную ширину;
   - бейджи, score и timeframe получили tabular/ellipsis-поведение;
   - action-кнопки в панели операций стали компактнее.

4. Улучшена адаптивность:
   - на desktop сохранён 3-колоночный cockpit;
   - на laptop правая аналитика корректно уходит ниже;
   - на tablet/mobile sticky-колонки отключаются;
   - карточки trade sheet и execution map используют auto-fit.

5. JS-правки:
   - начальное состояние `opsPanel` теперь свернуто;
   - `decisionBoard` сохраняет класс `signal-card` при перерендере;
   - текст состояния ops-панели приведён к новой логике.

## Проверки

- `node --check frontend/app.js` — OK
- duplicate HTML ids — отсутствуют
- JS DOM refs — все найдены в HTML
- button без type — отсутствуют
